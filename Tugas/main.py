import subprocess

# Mengeksekusi skrip 1
subprocess.run(["python", "socket_timeout.py"])

# Mengeksekusi skrip 2
subprocess.run(["python", "connection_status.py"])

# Mengeksekusi skrip 3
subprocess.run(["python", "socket_buffer.py"])